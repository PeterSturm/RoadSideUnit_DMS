@page
@model DashboardWebApp.Pages.RSUs.DeleteModel
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>RSU</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.RSU.Name)</dt>
        <dd class="col-sm-8">@Html.DisplayFor(model => model.RSU.Name)</dd>

        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.RSU.IP)</dt>
        <dd class="col-sm-8">@Model.RSU.IP.ToString()</dd>

        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.RSU.Port)</dt>
        <dd class="col-sm-8">@Html.DisplayFor(model => model.RSU.Port)</dd>

        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.RSU.Latitude)</dt>
        <dd class="col-sm-8">@Html.DisplayFor(model => model.RSU.Latitude)</dd>

        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.RSU.Longitude)</dt>
        <dd class="col-sm-8">@Html.DisplayFor(model => model.RSU.Longitude)</dd>

        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.RSU.LocationDescription)</dt>
        <dd class="col-sm-8">@Html.DisplayFor(model => model.RSU.LocationDescription)</dd>
    </dl>

    <hr />

    <form method="post">
        <input type="hidden" asp-for="RSU.Id" />
        <input type="hidden" asp-for="RSU.Manager.Id" />
        <input type="submit" value="Delete" class="btn" />
        <a class="p-3" asp-page="./Index">Back to List</a>
    </form>
</div>

